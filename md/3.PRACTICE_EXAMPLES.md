# 实践示例：从 Hello World 到交互式应用

> 通过实际修改代码来学习 Android 开发

## 示例 1：修改文本内容

### 步骤 1：找到并修改 Greeting 函数

打开 `app/src/main/java/com/example/comicreader/MainActivity.kt`

找到这个函数：

```kotlin
@Composable
fun Greeting(name: String, modifier: Modifier = Modifier) {
    Text(
        text = "Hello $name!",
        modifier = modifier
    )
}
```

### 步骤 2：修改文本

```kotlin
@Composable
fun Greeting(name: String, modifier: Modifier = Modifier) {
    Text(
        text = "欢迎使用漫画阅读器！$name",  // 修改这里
        modifier = modifier
    )
}
```

### 步骤 3：查看效果

1. 在 Android Studio 中，找到右侧的 Preview 面板
2. 如果看不到，点击代码右上角的 "Split" 或 "Design" 图标
3. 点击 Preview 面板上的刷新按钮（🔄）

---

## 示例 2：添加一个简单的计数器

### 完整代码

替换 `MainActivity.kt` 中的 `Greeting` 函数：

```kotlin
@Composable
fun Greeting(name: String, modifier: Modifier = Modifier) {
    // 状态变量（类似 React 的 useState）
    var count by remember { mutableStateOf(0) }
    
    // 垂直布局（类似 flex-col）
    Column(
        modifier = modifier.padding(16.dp),
        horizontalAlignment = Alignment.CenterHorizontally
    ) {
        Text(
            text = "Hello $name!",
            fontSize = 24.sp,
            modifier = Modifier.padding(bottom = 16.dp)
        )
        
        Text(
            text = "Count: $count",
            fontSize = 32.sp,
            fontWeight = FontWeight.Bold,
            modifier = Modifier.padding(bottom = 16.dp)
        )
        
        // 水平布局（类似 flex-row）
        Row(
            horizontalArrangement = Arrangement.spacedBy(16.dp)
        ) {
            Button(onClick = { count++ }) {
                Text("+")
            }
            Button(onClick = { count-- }) {
                Text("-")
            }
            Button(onClick = { count = 0 }) {
                Text("Reset")
            }
        }
    }
}
```

### 需要的导入

在文件顶部添加（如果 IDE 没有自动添加）：

```kotlin
import androidx.compose.foundation.layout.Column
import androidx.compose.foundation.layout.Row
import androidx.compose.foundation.layout.Arrangement
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Button
import androidx.compose.ui.Alignment
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.dp
import androidx.compose.ui.unit.sp
import androidx.compose.runtime.getValue
import androidx.compose.runtime.setValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
```

### 运行和测试

1. 在 Android Studio 中点击运行按钮（绿色三角形）
2. 或使用快捷键 `Shift + F10`
3. 在模拟器或真机上测试按钮功能

---

## 示例 3：修改主题颜色

### 步骤 1：打开颜色文件

打开 `app/src/main/java/com/example/comicreader/ui/theme/Color.kt`

### 步骤 2：查看当前颜色定义

```kotlin
val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
```

### 步骤 3：修改为你喜欢的颜色

```kotlin
// 使用在线工具将十六进制颜色转为 0xFF 格式
// 例如：蓝色 #2196F3 -> 0xFF2196F3

val Blue80 = Color(0xFF90CAF9)      // 浅蓝
val BlueGrey80 = Color(0xFFB0BEC5)  // 浅灰蓝
val Teal80 = Color(0xFF80CBC4)      // 浅青色

val Blue40 = Color(0xFF1976D2)      // 深蓝
val BlueGrey40 = Color(0xFF455A64)  // 深灰蓝
val Teal40 = Color(0xFF00796B)      // 深青色
```

### 步骤 4：更新 Theme.kt

打开 `ui/theme/Theme.kt`，替换颜色引用：

```kotlin
private val DarkColorScheme = darkColorScheme(
    primary = Blue80,        // 替换 Purple80
    secondary = BlueGrey80,  // 替换 PurpleGrey80
    tertiary = Teal80        // 替换 Pink80
)

private val LightColorScheme = lightColorScheme(
    primary = Blue40,        // 替换 Purple40
    secondary = BlueGrey40,  // 替换 PurpleGrey40
    tertiary = Teal40        // 替换 Pink40
)
```

### 步骤 5：查看效果

在 Preview 中查看新的颜色主题

---

## 示例 4：创建一个列表

### 代码

在 `MainActivity.kt` 中添加新函数：

```kotlin
@Composable
fun ComicList(modifier: Modifier = Modifier) {
    // 数据列表（类似数组）
    val comics = listOf(
        "海贼王",
        "火影忍者",
        "进击的巨人",
        "鬼灭之刃",
        "咒术回战"
    )
    
    Column(modifier = modifier.padding(16.dp)) {
        Text(
            text = "我的漫画列表",
            fontSize = 24.sp,
            fontWeight = FontWeight.Bold,
            modifier = Modifier.padding(bottom = 16.dp)
        )
        
        // 遍历列表（类似 map）
        comics.forEach { comic ->
            Card(
                modifier = Modifier
                    .fillMaxWidth()
                    .padding(vertical = 4.dp),
                onClick = { /* 点击事件 */ }
            ) {
                Text(
                    text = comic,
                    modifier = Modifier.padding(16.dp),
                    fontSize = 16.sp
                )
            }
        }
    }
}
```

### 需要的导入

```kotlin
import androidx.compose.foundation.layout.fillMaxWidth
import androidx.compose.material3.Card
import androidx.compose.foundation.clickable
```

### 使用

在 `MainActivity` 的 `setContent` 中：

```kotlin
setContent {
    ComicReaderTheme {
        Scaffold(modifier = Modifier.fillMaxSize()) { innerPadding ->
            ComicList(modifier = Modifier.padding(innerPadding))
        }
    }
}
```

---

## 示例 5：使用字符串资源（国际化准备）

### 步骤 1：添加字符串资源

打开 `app/src/main/res/values/strings.xml`

```xml
<resources>
    <string name="app_name">Comic Reader</string>
    <string name="welcome_message">欢迎使用漫画阅读器</string>
    <string name="counter_label">计数：%1$d</string>
    <string name="increment">增加</string>
    <string name="decrement">减少</string>
    <string name="reset">重置</string>
</resources>
```

### 步骤 2：在代码中使用

```kotlin
import androidx.compose.ui.res.stringResource

@Composable
fun Greeting(modifier: Modifier = Modifier) {
    Text(
        text = stringResource(R.string.welcome_message),
        modifier = modifier
    )
}
```

---

## 调试技巧

### 1. 使用 Log 打印信息

```kotlin
import android.util.Log

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        Log.d("MainActivity", "onCreate called")  // 在 Logcat 中查看
        
        setContent {
            // ...
        }
    }
}
```

### 2. 在 Logcat 中过滤日志

- 打开底部的 "Logcat" 标签
- 在搜索框输入 "MainActivity" 过滤
- 使用下拉菜单选择日志级别（Verbose, Debug, Info, Warn, Error）

### 3. 添加断点调试

1. 在代码行号左侧点击，添加红色圆点（断点）
2. 点击调试按钮（绿色虫子图标）运行
3. 程序会在断点处暂停
4. 可以查看变量值、单步执行等

---

## 常见错误和解决方案

### 错误 1：Cannot resolve symbol 'remember'

**解决：** 添加导入
```kotlin
import androidx.compose.runtime.remember
import androidx.compose.runtime.mutableStateOf
```

### 错误 2：Preview 显示错误

**解决：** 
- 确保 `@Preview` 注解存在
- 点击 "Build > Clean Project"，然后 "Build > Rebuild Project"

### 错误 3：Gradle sync failed

**解决：**
- 检查网络连接
- `File > Invalidate Caches / Restart`
- 如果修改了 `build.gradle.kts`，确保语法正确

### 错误 4：应用崩溃

**解决：**
- 查看 Logcat 中的红色错误信息
- 检查是否有空指针异常（NullPointerException）
- 检查 AndroidManifest.xml 中的权限声明

---

## 下一步学习方向

1. ✅ 修改现有组件
2. ✅ 添加状态管理
3. ✅ 创建列表
4. 🔜 学习导航（页面跳转）
5. 🔜 学习网络请求（类似 axios/fetch）
6. 🔜 学习数据持久化（类似 localStorage）
7. 🔜 学习权限管理
8. 🔜 学习图片加载（类似 img 标签）

---

## 有用的资源

- **颜色选择器：** https://material.io/resources/color/
- **Compose 官方示例：** https://github.com/android/compose-samples
- **Kotlin Playground：** https://play.kotlinlang.org/（在线练习 Kotlin 语法）

